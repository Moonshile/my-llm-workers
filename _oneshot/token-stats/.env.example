# 保密/隐私配置（非保密配置在 worker.yaml 中）

# Session 文件搜索路径，逗号分隔（属于个人路径，故放在 .env）
# 默认: ~/.claude/projects
SESSION_DIRS=~/.claude/projects

# Session 元数据目录，用于获取 session 名称映射
# 默认: ~/.claude/sessions
SESSION_META_DIR=~/.claude/sessions
