[project]
name = "my-llm-workers"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "apscheduler>=3.10,<4",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = ["pytest>=8.0"]
