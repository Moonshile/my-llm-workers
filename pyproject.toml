[project]
name = "my-llm-workers"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "apscheduler>=3.10,<4",
    "chinesecalendar>=1.10",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
    "litellm>=1.88.1",
]

[dependency-groups]
dev = ["pytest>=8.0"]
