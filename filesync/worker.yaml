name: filesync
description: 按 mtime 将文件组内最新文件同步到其他位置，覆盖前自动备份（30天滚动删除）
language: python
log_retention_days: 7
schedule:
  enabled: true
  cron: "*/5 * * * *"
  job_options:
    misfire_grace_time: 60
    coalesce: true
    max_instances: 1
run: "python main.py"
